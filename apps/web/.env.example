# Endereço da API (apps/api). O Next repassa /api/... para ele (rewrite em next.config.ts).
# Na Vercel, apontar para a URL pública da API (ex.: https://portal-api.up.railway.app).
API_ORIGIN="http://localhost:3333"
